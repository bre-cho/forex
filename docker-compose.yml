version: '3.9'

# Root compose intentionally does not host legacy ./backend or ./frontend stacks.
# Use one of the infra entry points instead:
#   docker compose -f infra/docker/docker-compose.dev.yml up -d
#   docker compose -f infra/docker/docker-compose.prod.yml up -d
services: {}
