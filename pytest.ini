[pytest]
testpaths =
    apps/api/tests
    services/trading-core/tests
pythonpath =
    apps/api
    services/trading-core
markers =
    smoke: live sandbox smoke tests that require external credentials
    asyncio: async tests (requires pytest-asyncio plugin)
